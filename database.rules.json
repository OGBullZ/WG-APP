{
  "rules": {
    ".read": false,
    ".write": false,
    "wg": {
      "$code": {
        ".read":  "$code.length >= 6 && $code.length <= 64",
        ".write": "$code.length >= 6 && $code.length <= 64"
      }
    }
  }
}
